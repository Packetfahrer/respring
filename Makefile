TWEAK_NAME = respring
respring_FILES = Tweak.xm
respring_FRAMEWORKS = Foundation UIKit
respring_PRIVATE_FRAMEWORKS = AppSupport

include $(THEOS)/makefiles/common.mk
include $(THEOS)/makefiles/tweak.mk
